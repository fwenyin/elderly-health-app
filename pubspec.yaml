name: namer_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  firebase_auth: ^4.12.0
  firebase_core: ^2.20.0
  intl_phone_field: ^3.1.0
  cloud_firestore: ^4.12.1
  http: ^1.1.0
  url_launcher: ^6.0.0
  csv: ^5.0.0
  shared_preferences: ^2.0.6
  intl_translation: ^0.18.2
  intl: ^0.18.1
  intl_utils: ^2.8.5


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  generate: true
  uses-material-design: true

  assets:
    - lib/asset/human_figure.png
    - lib/data/preprocessed_medical_kb.csv